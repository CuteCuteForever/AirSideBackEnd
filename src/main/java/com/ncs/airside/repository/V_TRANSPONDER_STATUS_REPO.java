package com.ncs.airside.repository;

import com.ncs.airside.model.view.V_TRANSPONDER_STATUS;
import org.springframework.data.jpa.repository.JpaRepository;

public interface V_TRANSPONDER_STATUS_REPO extends JpaRepository<V_TRANSPONDER_STATUS, Long> {
}